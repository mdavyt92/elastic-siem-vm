[Unit]
Description=logstash
Requires=docker.service network-online.target
After=docker.service network-online.target

[Service]
WorkingDirectory=/opt/docker-compose
Type=simple
TimeoutStartSec=15min
Restart=always

ExecStartPre=/usr/bin/docker-compose -f logstash.yml pull --quiet --ignore-pull-failures
ExecStartPre=/usr/bin/docker-compose -f logstash.yml build --pull

ExecStart=/usr/bin/docker-compose -f logstash.yml up --remove-orphans

ExecStop=/usr/bin/docker-compose -f logstash.yml down --remove-orphans

ExecReload=/usr/bin/docker-compose -f logstash.yml pull --quiet --ignore-pull-failures
ExecReload=/usr/bin/docker-compose -f logstash.yml build --pull

[Install]
WantedBy=multi-user.target
